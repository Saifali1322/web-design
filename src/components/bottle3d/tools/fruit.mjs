/**
 * Cutting the fruit out of the product photography.
 *
 * Build-time only. Placeholder while the crops are chosen — `FRUIT_CUTS` is
 * filled in below once each piece has been picked off the frame.
 */

/** @type {Record<string, Array<{ x: number, y: number, r: number, place: object }>>} */
export const FRUIT_CUTS = {};

export function cutFruit() {
  throw new Error("not implemented yet");
}
